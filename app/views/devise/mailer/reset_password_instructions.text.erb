<%= @resource.email %> 様

culture-linkをご利用いただきありがとうございます。
パスワード再設定のリクエストを受け付けました。

以下のリンクをクリックして、新しいパスワードを設定してください。

<%= edit_password_url(@resource, reset_password_token: @token) %>

このリンクの有効期限は発行から<%= Devise.reset_password_within.to_i / 3600 %>時間です。

心当たりのない場合は、このメールを破棄してください。
上記のリンクをクリックするまで、パスワードは変更されません。

---
このメールはculture-linkのシステムから自動送信されています。
お問い合わせ: supportculturelink@gmail.com
